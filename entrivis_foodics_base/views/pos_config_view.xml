<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="sync_pos_config_form" model="ir.ui.view">
            <field name="name">sync.pos.config.form</field>
            <field name="model">pos.config</field>
            <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//form/sheet" position="before">
                    <header>
                        <button class="oe_highlight" name="sync_specific_branch" string="Sync Branch" type="object"/>
                    </header>
                </xpath>
                <xpath expr="//div[@id='title']" position="after">
                    <div id="foodics_id">
                        <label for="foodics_id"/>
                        <field name="foodics_id" readonly="1"/>
                    </div>
                </xpath>
            </field>
        </record>

        <record id="action_sync_pos_config" model="ir.actions.server">
            <field name="name">Sync Pos Config</field>
            <field name="model_id" ref="point_of_sale.model_pos_config"/>
            <field name="binding_model_id" ref="point_of_sale.model_pos_config"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">
                action = records.sync_all_branch()
            </field>
        </record>
    </data>
</odoo>
